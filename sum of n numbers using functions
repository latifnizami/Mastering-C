#include <stdio.h>
#include<math.h>

float sum(int n);
float sum(int n)
{
    float totsum=5.6;

for(char i=0;i<=n;i++)
{
    
    totsum=totsum+i;
    
}
return totsum;    
}

int main()
{
    float val=sum(11.22);
    
    printf("Total sum is:%f",val);

    return 0;
}
